/// Common types shared across the LapSphere workspace.
///
/// This crate contains all data structures used for system information,
/// hardware telemetry, configuration, and IPC between the daemon, GUI,
/// and D-Bus interface. It has no dependencies on the other crates.
pub mod types;