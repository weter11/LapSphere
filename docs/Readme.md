# Subsystem docs

> STATUS: NOT YET POPULATED (no subsystem files exist yet)
> Agents: this is not an error — proceed with extra caution and say so.

Add a file here only for a subsystem complex enough to justify one — a single overview/modules/invariants split usually isn't enough detail for it. `daemon/src/hardware_detection.rs` (4,130 lines, handles gamepad/device enumeration) is the obvious first candidate given known bug history.
